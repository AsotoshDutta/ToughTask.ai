rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // User Profile Rules - Allow username-based access
    match /users/{userId} {
      allow read, write: if true;
    }

    // Task Rules - Allow access to tasks
    match /tasks/{taskId} {
      allow read, write: if true;
    }
  }
}
